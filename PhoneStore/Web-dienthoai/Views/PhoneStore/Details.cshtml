@model Web_dienthoai.Models.SanPham


@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_MasterLayout2.cshtml";
}

<link rel="stylesheet" href="~/Content/bootstrap.min.css" />
@*<link rel="stylesheet" href="~/Content/Image.css" />
<link rel="stylesheet" href="~/Content/mystyle.css" />*@
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>


<div class="container">
    <div class="row">
        <div class="col-md-6">
            <div class="slideshow-container">
                @*@foreach (var item in Model.HinhSP)
                {*@
                    <div class="mySlides @*fade*@">
                        <img src="~/Images/ProductView/@Model.HinhMinhHoa" style="width: 100%; height: 500px;">
                    </div>
                @*}*@
            </div>
            <br>
            <div style="text-align:center">
                <span class="dot"></span>
                <span class="dot"></span>
                <span class="dot"></span>
                <span class="dot"></span>
                <span class="dot"></span>
            </div>
        </div>
        <div class="col-md-6">

            <p class="price" style="text-align: center;">
                <span style="font-size: 34px; padding: 5px;  "> <strong>Giá bán:</strong> @String.Format("{0:0,0}", @Model.Gia) VND</span>
            </p>



            <h2 style="text-align:center; ">CHI TIẾT SẢN PHẨM</h2>
            <hr />
            <div style="padding-left: 0px; padding-right: 0px;">
                <h3 style="  text-align: center; ">@Model.TenSP</h3>
                <p style="  padding-left: 0px; padding-right: 0px; text-align: justify ">
                    <span style="  font-weight: bold; text-align: center; ">Mô tả:</span> @Model.MoTaSP
                </p>
                <hr />

                <div style="display:flex">
                    <a class="btn btn-primary" style="margin:0px auto;height:40px;width:100%;font-weight:bold;text-align:center" href="@Url.Action("ThemSP", "GioHang", new { IdSP = Model.IdSP })"> Đặt mua </a>
                </div>

                <br />

                <h4 style=" ">THÔNG SỐ KỸ THUẬT:</h4>
                <div class="thongso">
                    <div class="accordion" id="accordionExample">
                        <div class="accordion-item">
                            <h2 class="accordion-header" id="headingOne">
                                <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
                                    Thông số kỹ thuật
                                </button>
                            </h2>
                            <div id="collapseOne" class="accordion-collapse collapse show" aria-labelledby="headingOne" data-bs-parent="#accordionExample">
                                <div class="accordion-body">
                                    @foreach (var item in Model.ThongSoes)
                                    {
                                        <div class="form-group">
                                            <strong>@item.TenTS:</strong>
                                            <div class="col-md-10">
                                                @item.Mota
                                            </div>
                                        </div>
                                    }
                                </div>
                            </div>
                        </div>
                     </div>
                </div>


                        @*@Html.Action("Index", "ThongSoes", new { id = Model.IdSP })*@


                    </div>
                </div>

            </div>
    </div>
</div>

<hr class="" />
<div style="border-radius: 5px; margin: 10px auto;padding:10px" class="w-50 ">
    <h3 style="text-align:center" class="">Đánh giá Điện thoại @Model.TenSP  </h3>
    <hr />
    <div style="text-align:center">

        @*@Html.ActionLink("Viết bình luận", "BinhLuanKH", "PhoneStore", null, new { @class = "btn btn-secondary" })
            </div><br />
            <p style="text-align:center">@ViewBag.thongbao</p>
            @foreach (var item in Model.BinhLuan)
            {
                <p><b>@item.HoTen</b> | @item.ThoiGian</p> <br />
                <img class="w-25" src="~/Images/BinhLuan/@item.Anh" alt="Lỗi Hình"><br /><br />
                <p>@item.Chiase</p>
                <hr />
            }*@


    </div>



    @*<script>
            let slideIndex = 0;
            let slideInterval;

            function startSlideShow() {
                slideInterval = setInterval(showSlides, 2000);
            }

            function stopSlideShow() {
                clearInterval(slideInterval);
            }

            function showSlides() {
                let i;
                let slides = document.getElementsByClassName("mySlides");
                let dots = document.getElementsByClassName("dot");
                for (i = 0; i < slides.length; i++) {
                    slides[i].style.display = "none";
                }
                slideIndex++;
                if (slideIndex > slides.length) { slideIndex = 1 }
                for (i = 0; i < dots.length; i++) {
                    dots[i].className = dots[i].className.replace(" active", "");
                }
                slides[slideIndex - 1].style.display = "block";
                dots[slideIndex - 1].className += " active";

            }

            startSlideShow();

        </script>*@

    <script>
        $(document).ready(function () {
            // Add minus icon for collapse element which
            // is open by default
            $(".collapse.show").each(function () {
                $(this).prev(".card-header").find(".fa")
                    .addClass("fa-minus").removeClass("fa-plus");
            });
            // Toggle plus minus icon on show hide
            // of collapse element
            $(".collapse").on('show.bs.collapse', function () {
                $(this).prev(".card-header").find(".fa")
                    .removeClass("fa-plus").addClass("fa-minus");
            }).on('hide.bs.collapse', function () {
                $(this).prev(".card-header").find(".fa")
                    .removeClass("fa-minus").addClass("fa-plus");
            });
        });
    </script>
